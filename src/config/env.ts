/**
 * Variables d'environnement de l'application.
 * @property {string} apiUrl - URL de base de l'API backend (ex: http://localhost:3000)
 */
export const env = {
  apiUrl: process.env.NEXT_PUBLIC_API_URL as string,
};
